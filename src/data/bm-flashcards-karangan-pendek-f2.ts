// ─── BM Flashcards — Karangan Pendek (Form 2) ──────────────────────────────────────
// Scaffolding only — mirrors the Form 1 structure (src/data/bm-flashcards-karangan-pendek.ts)
// so the Form 2 chapter exists and renders correctly. No cards yet: waiting
// on real Form 2 KSSM content before anything is added here (never
// fabricated — see the registry entry this feeds for the same note).

import type { Flashcard } from "./types";

export const bmFlashcardsKaranganPendekF2: Flashcard[] = [];
